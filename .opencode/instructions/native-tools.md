# Native Tool Preference

For repository inspection, use the native `read`, `list`, `glob`, and `grep` tools.

Do not use Bash to read files, list directories, find files, or search content.

Use Bash only when a native tool cannot perform the task.
